What would be the short version?

First, let's use `:` to denote the types of the objects:

```text
A : Chair
B : Chair

AR : Backrest
AS : Seat
AL : Legs

BR : Backrest
BS : Seat
BL : Legs
```

But how would you describe that chair `A`

Yet again, the backrest of chair A is different from the backrest of chair B. It's the same situation:

- `AR` is a backrest of chair `A`.
- `BR` is a backrest of chair `B`.
