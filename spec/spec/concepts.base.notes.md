# Notes

* `===` means `Eq` (propositional equality)
* `|` is a pipeline (passes lhs as the last argument to rhs)
* `#[]` is a newline-separated list
  * Maybe we should make lists newline-separated by default
